# DUP-018 — Enforce one primary inferential key; keep nickname-based views as sensitivity analyses only

**Priority:** P2  
**Status:** Done (2026-03-01)

## Goal
Ensure that inferential outputs, timing views, and report filters are not silently talking about different entity definitions.

## Scope
- Choose one primary inferential key for collision inference.
- Recommended default: strict full-name key.
- Keep nickname-based grouping as a sensitivity view only.
- Align timing and report displays to the active inferential key by default.

## Constraints
- No phonetic matching.
- No spelling correction.
- Nickname-table expansion is allowed.
- Other fuzzy matching is out of scope.

## Failure modes and mitigations
- **Failure mode:** users compare a strict inferential result to a nickname-based timing table and assume they refer to the same grouping.  
  **Mitigation:** sensitivity badges and explicit key labels in all tables.
- **Failure mode:** engineers add new loose modes that exceed project policy.  
  **Mitigation:** config guardrails and tests.

## Acceptance criteria
- Every inferential result states which key generated it.
- Sensitivity views are clearly separated from primary inference.
- No hidden key-mode switching occurs in the UI.

## Verification
- Payload tests for consistent key labeling.
- UI tests for sensitivity badges.
- Regression tests blocking unauthorized fuzzy matching modes.

## Implementation summary
- Enforced a single inferential key in `duplicates_exact`:
  - detector now requires `collision_key_mode="strict"` and raises on non-strict inferential key configuration.
  - strict timing/per-name mode is aligned to the active inferential key column.
- Fixed strict timing mode drift:
  - strict top-name/per-name timing mode no longer uses `collision_key_medium`; it is tied to strict inferential key semantics.
- Added explicit primary-vs-sensitivity metadata across duplicate mode tables:
  - `match_mode_role` (`primary_inferential` / `sensitivity_only`)
  - `inferential_key_mode` (`strict`)
- Added inferential key provenance to inferential duplicate surfaces:
  - detector summary/statistical contract now include `inferential_key_mode`
  - inferential tables include `inferential_key_mode` (scope/bucket/per-name/temporal/runtime pathways)
- Added payload/control contract fields for UI clarity:
  - `controls.duplicate_inferential_key_mode`
  - `controls.duplicate_inferential_key_label`
  - `controls.duplicate_match_mode_policy`
  - duplicate statistical contract/declarations now include inferential key + mode policy.
- Updated duplicate UI controls:
  - duplicate match-mode options now render explicit policy labels:
    - `Strict (Primary inferential key)`
    - `Loose (nickname) (Sensitivity view)`
  - added duplicate match-mode status badge that toggles:
    - `Primary inferential key` for strict
    - `Sensitivity view` for loose
  - declaration notes now state inferential key explicitly.

## Validation completed
- Tests added/updated:
  - `tests/test_duplicates_exact.py`
    - `test_collision_key_mode_guard_rejects_non_strict_inferential_mode`
    - expanded `test_top_name_timing_by_mode_emits_ranked_rows_with_expected_mode_collapsing` for `match_mode_role`/`inferential_key_mode`
  - `tests/test_report_chart_payload.py`
    - control/contract assertions for inferential-key and match-mode-policy fields
    - duplicate chart row assertions for `inferential_key_mode` and `match_mode_role`
- Focused suites passed:
  - `python -m pytest tests/test_duplicates_exact.py tests/test_report_chart_payload.py -q`
  - `python -m pytest tests/test_report_render_helpers.py tests/test_analysis_registry.py -q`
  - `python -m pytest tests/test_expected_duplicate_rate_fixtures.py -q`

## ESSB 6346 runtime + UI checks
- Run-all completed:
  - log: `output/run_logs/dup018_essb6346_runall_csv.log`
  - completion line:
    - `Run complete. Report: /Users/sayhiben/dev/legislature-tools/reports/ESSB6346-20260224-0800/report.html`
- Rerender completed:
  - log: `output/run_logs/dup018_essb6346_rerender.log`
  - completion line:
    - `Report written to: /Users/sayhiben/dev/legislature-tools/reports/ESSB6346-20260224-0800/report.html`
- Playwright MCP validation:
  - desktop: `1728x1117` (bucket/theme interactions, duplicate mode policy labels + badge state transitions)
  - mobile: `390x844` (global controls/sidebar toggle, bucket/theme interactions)
  - console/network:
    - only expected local `favicon.ico` `404`
    - report-data requests returned `200`.
