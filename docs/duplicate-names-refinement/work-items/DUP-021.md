# DUP-021 — Build a calibration harness and derive low-power thresholds from explicit operating targets

**Priority:** P2  
**Status:** Done (2026-03-01)

## Goal
Replace heuristic low-power rules with thresholds justified by documented false-positive and power targets.

## Scope
Build a reproducible simulation and evaluation harness covering:
- clean null hearings
- homonym-heavy legitimate samples
- injected repeated-name scenarios
- same-position concentration
- temporal bursts
- varying match coverage and missingness
- normalization sensitivity
- geography-conditioned VRDB variants

Use it to set:
- `low_power_min_unique_names`
- `low_power_min_expected_duplicates`
- family-specific gating thresholds
- any shipping thresholds for new VRDB sidecar outputs

## Failure modes and mitigations
- **Failure mode:** thresholds are chosen by intuition and drift over time.  
  **Mitigation:** encode targets such as minimum detectable effect and FDR goals.
- **Failure mode:** evaluation only checks formulas, not operating characteristics.  
  **Mitigation:** require simulation-based calibration reports as release artifacts.

## Acceptance criteria
- Low-power thresholds are justified by an explicit target.
- Calibration outputs are reproducible and versioned.
- Changes to inference code can be regression-tested against statistical operating benchmarks.

## Verification
- CI smoke calibration suite.
- Periodic full calibration run.
- Release gate on key operating-characteristic regressions.

## Implementation Summary
- Added calibration harness module:
  - `testifier_audit/src/testifier_audit/backtests/duplicate_low_power_calibration.py`
- Added runnable calibration script:
  - `testifier_audit/scripts/tests/calibrate_duplicate_low_power.py`
- Added family-specific low-power config support and detector wiring:
  - `low_power_min_unique_names_scope`
  - `low_power_min_expected_duplicates_scope`
  - `low_power_min_unique_names_bucket`
  - `low_power_min_expected_duplicates_bucket`
  - `low_power_min_unique_names_position`
  - `low_power_min_expected_duplicates_position`
- Updated duplicate detector outputs and report payload contracts to publish active threshold provenance.
- Updated calibrated defaults in:
  - `testifier_audit/configs/default.yaml`
  - `testifier_audit/configs/voter_registry_enabled.yaml`

## Calibration Artifact Baseline (DUP-021)
- Artifact directory:
  - `output/dup021/`
- Baseline run command:
  - `python testifier_audit/scripts/tests/calibrate_duplicate_low_power.py --out-dir output/dup021 --scenario-replicates 24 --scope-draws 256 --bucket-draws 128 --position-permutations 400 --seed 6346`
- Selected threshold recommendations:
  - global/scope: `low_power_min_unique_names=10`, `low_power_min_expected_duplicates=1.0`
  - bucket: `low_power_min_unique_names_bucket=10`, `low_power_min_expected_duplicates_bucket=1.0`
  - position: `low_power_min_unique_names_position=10`, `low_power_min_expected_duplicates_position=1.0`

## Outcome Against Targets
- Bucket family met the defined operating targets in the baseline run.
- Scope and position families did not meet all power targets in the baseline run; this is now explicit in versioned calibration outputs (`benchmark_summary`) rather than implicit in heuristic thresholds.

## Verification Completed
- Calibration smoke tests added and passing:
  - `testifier_audit/tests/test_duplicate_low_power_calibration.py`
- Detector/payload/config regression tests updated and passing.
- Full test suite pass:
  - `./testifier_audit/scripts/ci/test.sh` (`379 passed`)
- ESSB 6346 exercised via:
  - unified report generation logs (`output/dup021/essb6346_unified*.log`)
  - rerender log (`output/dup021/essb6346_rerender.log`)
  - Playwright MCP desktop/mobile validation (controls, bucket/theme propagation, console/network diagnostics).
