# DUP-004 — Implement a dedicated VRDB collision-null engine per analysis slice

**Priority:** P0  
**Status:** Done (2026-02-28)  
**Depends on:** DUP-002, DUP-003

## Goal
Add a new null-model layer that answers one specific question:

> Given a slice of size N and a VRDB-derived name distribution, how many duplicate name-string collisions should occur by chance?

## Scope
For each slice (full hearing, bucket, position slice, time slice, geography slice, etc.), compute:
- observed duplicate pairs
- observed number of unique names
- observed max repeat count for a single normalized name
- expected duplicate pairs under the selected VRDB baseline
- expected count per specific name where needed
- Monte Carlo reference bands and tail probabilities

Minimum required metric:
- `expected_pairs = C(N_slice, 2) * Σ p_i^2`

Monte Carlo outputs should include, at minimum:
- mean/median expected pairs
- p95 and p99
- tail probability for observed pairs
- optional max-repeat reference bands if stable enough

## Design rules
- Compute each slice with that slice’s own `N_slice`.
- Do **not** linearly allocate a global duplicate expectation down into buckets.
- Treat outputs strictly as **string-collision** expectations, not person-duplication estimates.

## Failure modes and mitigations
- **Failure mode:** slice-level expectations are derived by rescaling a hearing-wide result.  
  **Mitigation:** each slice must run the null independently with its own N and baseline variant.
- **Failure mode:** small buckets create highly discrete, unstable tail behavior.  
  **Mitigation:** low-power/descriptive-only gating and explicit Monte Carlo precision metadata.
- **Failure mode:** “max repeated name” is interpreted too literally in sparse slices.  
  **Mitigation:** mark it secondary/descriptive unless calibrated.

## Acceptance criteria
- Per-slice expectations are computed directly from slice N and selected VRDB probabilities.
- The engine produces additive outputs without modifying existing detectors.
- All outputs are labeled as VRDB-derived collision-null metrics.

## Verification
- Closed-form toy-case tests for expected pairs.
- Simulation parity tests against direct multinomial draws from known probability vectors.
- Regression tests showing that bucket expectations are not derived from a linearly rescaled global expectation.

## Implementation Notes (Completed)
- Added a dedicated VRDB collision-null engine module:
  - `testifier_audit/src/testifier_audit/io/vrdb_collision_null.py`
  - provides:
    - slice-level null computation (`compute_vrdb_collision_null_for_slices`)
    - probability/backoff artifact loading and collision-table writing helpers
    - deterministic seed derivation per slice for reproducible Monte Carlo references
    - geography backoff resolution support via DUP-003 backoff rows
- Implemented required per-slice outputs:
  - observed metrics:
    - duplicate pairs
    - unique name keys
    - max repeated-name count
  - expected metrics:
    - analytic expected pairs
    - closed-form expected pairs (`C(N,2) * Σ p_i^2`)
    - Monte Carlo mean/median/p95/p99 and one-sided empirical tail probability
  - inferential metadata:
    - `inferential_status` / `inferential_reason`
    - Monte Carlo draw accounting and reference-availability flags
  - additive evidence-family labeling:
    - `evidence_family = vrdb_collision_null`
- Added expected-per-name support:
  - top-name expected-count rows per slice with `observed_count`, `expected_count`, and `overrun_count`
- Added focused tests:
  - `testifier_audit/tests/test_vrdb_collision_null.py`
  - coverage includes:
    - closed-form toy-case expectation checks
    - Monte Carlo parity to multinomial expectation
    - regression proving bucket expectations are computed from bucket `N` (not linear rescale from full hearing)
    - geography backoff/effective-geo resolution
- Validation outcomes:
  - lint passed via `./testifier_audit/scripts/ci/lint.sh`
  - full suite passed via `./testifier_audit/scripts/ci/test.sh` (`339 passed`)
  - ESSB 6346 runtime exercise completed:
    - unified run (`run_unified_report.sh --skip-imports ...`)
    - rerender (`python -m testifier_audit.cli report ...`)
    - direct collision-null execution against ESSB slices with logs:
      - metrics output: `output/dup004/essb6346_vrdb_collision_metrics.csv`
      - expected-name output: `output/dup004/essb6346_vrdb_collision_expected_names.csv`
  - Playwright MCP validation completed (desktop + mobile):
    - bucket/theme/sidebar/global-control interactions verified
    - report-data shard requests returned `200`
    - only console error observed was expected local `favicon.ico` `404`.
