# DUP-011 — Define hypothesis families, gating, and multiplicity control across the detector

**Priority:** P1  
**Status:** Done (2026-02-28)

## Goal
Prevent dashboard-wide false positives by ensuring every inferential result belongs to an explicit hypothesis family with a stated correction rule.

## Scope
Implemented families:
- Family A: scope-level `excess_rows` tests (`A_scope_excess_rows`)
- Family B: bucket follow-ups (`B_bucket_follow_up`)
- Family C: per-name follow-ups (`C_per_name_upper_tail`)
- Family D: within-name temporal follow-ups (`D_temporal_follow_up`)
- Family E: legacy position follow-up (`E_position_follow_up`)

Implemented hierarchy:
- run Family B/C only if Family A passes for that scope
- run Family D only for names that pass Family C (per-name discoveries)

Store, at minimum:
- `family_id`
- `adjustment_method`
- `n_tests`
- `eligible_by_gate`
- `gate_reason`

## Failure modes and mitigations
- **Failure mode:** users infer significance by browsing many unadjusted outputs.  
  **Mitigation:** every inferential table must declare family and adjustment.
- **Failure mode:** downstream tables appear even when upstream evidence is absent.  
  **Mitigation:** enforce gating in the backend, not just the UI.

## Acceptance criteria
- Every inferential p-value belongs to exactly one family.
- Downstream families are gated by upstream results.
- Reports show test counts and correction method.

## Verification
- Detector/payload tests:
  - `tests/test_duplicates_exact.py`
  - `tests/test_report_chart_payload.py`
- Full suite:
  - `./testifier_audit/scripts/ci/test.sh` (`355 passed`)
- ESSB 6346 runtime exercise:
  - `run-all` recompute log: `output/run_logs/dup011_essb6346_runall_csv.log`
  - rerender log: `output/run_logs/dup011_essb6346_rerender.log`
  - Playwright MCP desktop/mobile validation completed with no console errors and no failed data requests.
