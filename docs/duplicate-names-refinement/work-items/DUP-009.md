# DUP-009 — Preserve scope semantics; remove silent fallbacks that change the estimand

**Priority:** P0  
**Status:** Completed (2026-02-28)

## Goal
Ensure that `full_hearing`, `matched_only`, and `unmatched_only` mean exactly what they say, or are marked unavailable.

## Problem
A scope that silently becomes some other scope is not a valid analysis target. If required match-assignment inputs are missing, or inference filters empty the frame, the correct behavior is “unavailable,” not substitution.

## Scope
- Disable `matched_only` and `unmatched_only` when match assignments are missing or malformed.
- If non-person exclusion empties the inference frame, mark the relevant scope unavailable.
- Add explicit `scope_status` and `scope_reason` fields.
- Keep `inferential_status`/`inferential_reason` from DUP-008 as separate fields; do not overload them for scope availability semantics.

## Failure modes and mitigations
- **Failure mode:** engineers preserve execution by copying `full_hearing` into `matched_only`.  
  **Mitigation:** hard guardrails and regression tests against aliasing.
- **Failure mode:** UI still offers unavailable scopes.  
  **Mitigation:** disable or hide with explanation tooltip.

## Acceptance criteria
- No scope silently aliases another scope.
- Unavailable scopes emit status/reason and no inferential metrics.
- UI and exports reflect actual scope availability.

## Verification
- Unit tests for missing assignments and empty filtered frames.
- Frontend integration tests for disabled scopes.
- Regression tests ensuring no fallback copies appear.

## Implementation Notes (2026-02-28)
- Implemented explicit scope availability semantics in detector output:
  - `scope_status`: `available` | `unavailable`
  - `scope_reason`: `available` | `unavailable_missing_match_assignments` | `unavailable_no_person_rows` | `unavailable_no_rows_after_filtering`
- Removed silent scope aliasing:
  - no fallback substitution of `matched_only`/`unmatched_only` with `full_hearing`
  - no fallback substitution when the inference frame is emptied by person filtering
- Kept inferential fields orthogonal to scope availability:
  - unavailable scopes map to inferential unavailability via `inferential_reason=scope_unavailable`
  - DUP-008 reason taxonomy remains intact for inferential-capable scopes
- Propagated scope metadata through detector tables, summary, payload, and duplicate control metadata.
- Updated duplicate scope UI plumbing so unavailable scopes are excluded from active options and can be explained via scope-availability metadata.

## Verification Evidence (2026-02-28)
- Unit/payload regressions:
  - `python -m pytest tests/test_duplicates_exact.py -q`
  - `python -m pytest tests/test_report_chart_payload.py -q`
- Full CI test script:
  - `./testifier_audit/scripts/ci/test.sh` (`328 passed`)
- Runtime validation on ESSB 6346:
  - unified run + rerender completed successfully
  - Playwright MCP desktop/mobile checks completed with clean console and network logs
